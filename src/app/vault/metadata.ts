import type { Metadata } from "next";
export const metadata: Metadata = { title: "Vault", description: "Deposit ETH or USDC into the CORTEX vault. Receive cVault shares and earn AI-managed yield on Base." };
