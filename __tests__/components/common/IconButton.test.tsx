import { render } from "@testing-library/react";
import IconButton from "@/components/common/IconButton";
describe("IconButton", () => { test("renders without crashing", () => { render(<IconButton />); }); });
