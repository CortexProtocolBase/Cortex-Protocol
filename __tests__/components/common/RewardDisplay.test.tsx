import { render } from "@testing-library/react";
import RewardDisplay from "@/components/common/RewardDisplay";
describe("RewardDisplay", () => { test("renders without crashing", () => { render(<RewardDisplay />); }); });
