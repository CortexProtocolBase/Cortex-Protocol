import { render } from "@testing-library/react";
import TextArea from "@/components/common/TextArea";
describe("TextArea", () => { test("renders without crashing", () => { render(<TextArea />); }); });
