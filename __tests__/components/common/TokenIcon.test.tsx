import { render } from "@testing-library/react";
import TokenIcon from "@/components/common/TokenIcon";
describe("TokenIcon", () => { test("renders without crashing", () => { render(<TokenIcon />); }); });
