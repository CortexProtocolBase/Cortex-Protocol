import { render } from "@testing-library/react";
import Dropdown from "@/components/common/Dropdown";
describe("Dropdown", () => { test("renders without crashing", () => { render(<Dropdown />); }); });
