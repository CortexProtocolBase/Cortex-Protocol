import { render } from "@testing-library/react";
import Timeline from "@/components/common/Timeline";
describe("Timeline", () => { test("renders without crashing", () => { render(<Timeline />); }); });
