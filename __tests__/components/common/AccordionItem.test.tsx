import { render } from "@testing-library/react";
import AccordionItem from "@/components/common/AccordionItem";
describe("AccordionItem", () => { test("renders without crashing", () => { render(<AccordionItem />); }); });
