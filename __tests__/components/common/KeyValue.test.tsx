import { render } from "@testing-library/react";
import KeyValue from "@/components/common/KeyValue";
describe("KeyValue", () => { test("renders without crashing", () => { render(<KeyValue />); }); });
